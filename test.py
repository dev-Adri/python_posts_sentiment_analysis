import json
import langdetect

# with open("posts.json", "r") as file:
#     data = json.load(file)
#     posts = data["posts"]
#     for post in posts:
#         if "text" in post:
#             text = post["text"]
#             print(langdetect.detect(text))